@model IEnumerable<Video>

<div class="card">
    <div class="card-body">
        <partial name="~/Views/Shared/Video/Grid.cshtml" model="@Model" />
        @*<div>
            <div class="btn btn-primary float-right"
                 data-toggle="modal" data-target="#video-modal">
                Xem tất cả
            </div>
        </div>*@
    </div>
</div>