@model IEnumerable<Album>
@{
    ViewData["Title"] = "Album";
}
<div class="row">
    @{
        foreach (var album in Model)
        {
        <div class="col-lg-3 mt-2 mb-2">
            <a asp-controller="Home" asp-action="Album" asp-route-id="@album.Id">
                <img class="img-fluid p-1 w-100 border" 
                     style="background-color:deeppink;"
                     src=@album.ImageUrl loading="lazy" aria-label="album" />
            </a>
            <ul class="d-flex list-unstyled mt-2 p-2">
                <li class="me-auto">
                    <a asp-controller="Home" 
                       asp-action="Artist" 
                       asp-route-id="@album.GetArtists()[0].Id">
                        <img src="@album.GetArtists()[0].ImageUrl" 
                             width="64" height="64" alt="artist" 
                             class="rounded-circle p-1"
                             style="background-color:limegreen;">
                    </a>
                </li>
                <li class="d-flex align-items-center">
                    <svg width="1em" height="1em">
                        <use xlink:href="#geo-fill"></use>
                    </svg>
                    <div>
                        <h6>@album.VietnameseName</h6>
                        <small class="text-secondary">
                            @album.SimplifiedChineseName
                        </small>
                    </div>
                </li>
            </ul>
        </div>
        }
    }
</div>

