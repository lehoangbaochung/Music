@{
    Layout = "~/Views/Shared/Layout/Profile.cshtml";
    ItemViewModel songViewModel = new()
    {
        Header = "Bài hát",
        Items = ViewBag.RecentSongs,
        Hyperlink = new("Home", "Artist", null, "Song")
    };
    ItemViewModel albumViewModel = new()
    {
        Header = "Album",
        Items = ViewBag.RecentAlbums,
        Hyperlink = new("Home", "Artist", null, "Album")
    };
    ItemViewModel videoViewModel = new()
    {
        Header = "Video",
        Items = ViewBag.RecentVideos,
        Hyperlink = new("Home", "Artist", null, "Video")
    };
}
<partial name="~/Views/Shared/Partial/List.cshtml" model="@songViewModel" />
<partial name="~/Views/Shared/Partial/Grid.cshtml" model="@albumViewModel" />
<partial name="~/Views/Shared/Partial/Grid.cshtml" model="@videoViewModel" />